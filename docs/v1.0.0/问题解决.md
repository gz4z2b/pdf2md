# v1.0.0 问题解决

## 本次处理内容

本文件记录 v1.0.0 开发过程中的问题、决策和改进点。

文档规划阶段已完成：

1. 根据 v1.0.0 需求确定项目架构。
2. 确定 Python 技术栈。
3. 补全 README 总索引。
4. 新增项目规范文档 `docs/rule.md`。
5. 新增测试规范文档 `docs/test.md`。
6. 新增功能说明文档 `docs/functions/pdf_batch_convert.md`。
7. 新增 v1.0.0 实现方案 `docs/v1.0.0/实现方案.md`。

开发实现阶段已完成：

1. 新增 `src/pdf2md` Python 源码包。
2. 新增 `scripts/convert.sh` 一键转换脚本。
3. 新增 `requirements.txt` 和 `requirements-dev.txt`。
4. 新增 pytest 自动化测试，覆盖目录扫描、PDF 读取、Markdown 生成、CLI 和脚本语法。
5. 新增 `.gitignore`，避免提交虚拟环境、输出结果、用户 PDF 和本地 CodeGraph 数据库。
6. 实现完成后重新运行 CodeGraph 索引，源码和测试文件已可被分析。

## 当前决策

| 问题 | 决策 |
| --- | --- |
| PDF 放在哪里 | 放在 `origin` 目录 |
| Markdown 输出到哪里 | 输出到 `output` 目录 |
| 是否递归子目录 | 是 |
| 是否导出图片 | v1.0.0 不导出，只写占位符 |
| 使用什么 PDF 库 | PyMuPDF |
| 是否做网页界面 | v1.0.0 不做 |
| 是否做 OCR | v1.0.0 不做 |
| 已存在输出文件 | 默认跳过，不覆盖，也不算失败 |

## 发现的问题

1. 初始仓库没有源代码，所以首次 CodeGraph 索引中没有可分析的代码节点；实现完成后已重新索引。
2. README 原来只有空索引，缺少架构、技术栈、功能说明和版本文档入口。
3. README 中已引用 `docs/rule.md` 和 `docs/test.md`，但这两个文件原来不存在。
4. 需求中没有明确输出目录，本次文档约定为 `output`，便于用户查找转换结果。

## 后续改进建议

1. 真实 PDF 的排版差异很大，v1.0.0 完成后应使用用户实际 PDF 做一次手动验收。
2. 如果用户后续需要扫描版 PDF，需要单独规划 OCR 版本。
3. 如果用户后续需要保留图片，需要单独规划图片导出目录和 Markdown 图片引用格式。
