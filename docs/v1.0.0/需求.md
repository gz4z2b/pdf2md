# 背景
- 需要有一个批量把pdf文档转换成md格式文本文件的程序

# 需求
1. 源文件会放在origin目录里
2. 遍历origin目录以及子目录里所有pdf文件，读取内容，然后转换成md格式的文档。需要尽力还原pdf里的排版。本期先做没有图片内容的转换。如果遇到图片直接给一个占位符到md文档里即可。
3. 输出的目录结构以及文件名保持与原来的一样。
4. 需要有一个一键执行的脚本放在scripts里，只需要执行脚本可以直接进行转换，包括前期环境检查安装等。